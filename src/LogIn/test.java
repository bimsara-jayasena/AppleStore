package LogIn;

import javax.swing.*;
import java.awt.*;

public class test extends JFrame {
    test(){
        setSize(500,520);
        getContentPane().setBackground(Color.green);
        setLocationRelativeTo(null);
        setVisible(true);
    }

}
